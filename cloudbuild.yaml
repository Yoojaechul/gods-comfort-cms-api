steps:
  # 1. Docker 이미지 빌드
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/cms-api:$COMMIT_SHA"
      - "."

  # 2. 이미지 푸시
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/cms-api:$COMMIT_SHA"

  # 3. Cloud Run 배포 (🔥 이게 핵심)
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      - "run"
      - "deploy"
      - "cms-api"
      - "--image"
      - "gcr.io/$PROJECT_ID/cms-api:$COMMIT_SHA"
      - "--region"
      - "asia-northeast3"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--port"
      - "8080"

images:
  - "gcr.io/$PROJECT_ID/cms-api:$COMMIT_SHA"
